# Socket/API server URL — uncomment only to override the defaults:
#   dev builds default to http://localhost:5000
#   production builds default to same-origin (single-service deploys)
# Leaving this set while building for production would bake localhost in.
# VITE_SERVER_URL=http://localhost:5000
